#include<stdio.h>
#include<math.h>
void main()
{
    int i=1,j,k,m,a,sum=1;
    printf("The day on which number of chickens have to be calculated\n");
   scanf("%d",&a);
    j=pow(3,a-1);
    printf("The total number of chickens are %d",j);
   //if(i>=6)
    //for(k=1;k<=a;k++)
  // {sum=sum+(2*k);}
  // m=j-sum;
  // printf("%d",m);
}
