#include <bits/stdc++.h>
using namespace std;

void main()
{
    cout << "Welcome" << endl;
    int size;
    cin >> size;
    int temp;
    vector<int> v;
    while(size--)
    {
        cout<< "Enter the number ";
        cin >> temp;
        v.push_back(temp);
    }
    int k;
    cin >> k;
    // cout << "O(nlogn)" << endl;
    // sort(v.begin(),v.end());
    // cout << v[k];
    ''' MIN HEAP ______ INCOMPLETE QUESTION '''
    return 0;
}